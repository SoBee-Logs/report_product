async def generate_diary(user_id: int) -> dict:
    """persona_transaction(C) 기반으로 LLM 일기 생성.
    일기 프롬프트 설계 후 구현 예정."""
    # TODO: persona_transaction JOIN transactions, photo_vlm_results, emotions_text 조회
    # TODO: 매핑된 결제 + 사진 description + emoji로 LLM 일기 생성
    # TODO: diary 테이블에 저장
    raise NotImplementedError("generate_diary: 일기 프롬프트 설계 후 구현")
